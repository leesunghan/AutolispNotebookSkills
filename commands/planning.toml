description = "작업을 CAD 데이터베이스 인수 조건을 포함한 검증 가능한 소규모 작업으로 분할"

prompt = """
planning-and-task-breakdown 스킬을 호출합니다.

기존 명세서(projects/<프로젝트명>/SPEC.md) 및 CAD 환경 제약 사항을 검토한 후 다음 단계를 진행하십시오:

1. 계획 모드(Plan Mode)로 진입 — 코드 변경 없이 읽기 전용으로 탐색합니다.
2. AutoLISP 헬퍼 함수, DCL 레이아웃, 핵심 루틴 간의 의존성 그래프를 분석합니다.
3. 작업을 수직 슬라이스로 분할합니다 (셀 단위의 진행 순서: 마크다운 설명 셀 -> progn으로 래핑된 실행 가능한 AutoLISP 코드 셀 -> 검증 어설션 셀).
4. 세부 CAD 인수 조건(예: 예상 객체 유형, 레이어 설정, 기하학적 좌표 등) 및 검증 단계가 포함된 개별 작업을 구체적으로 작성합니다.
5. 실행 취소 마커(vla-StartUndoMark / vla-EndUndoMark)와 환경 복원 체크포인트를 설정합니다.
6. 작성된 계획을 검토할 수 있도록 사용자에게 제시합니다.

계획은 projects/<프로젝트명>/plan.md에, 세부 작업 목록은 projects/<프로젝트명>/todo.md에 저장하십시오.
"""