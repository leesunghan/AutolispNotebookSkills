description = "AutoLISP 코드의 가독성과 유지보수성 향상 — 동작 변경 없이 복잡도 감소"

prompt = """
code-simplification 스킬을 호출합니다.

정확한 동작은 유지하면서 최근에 수정된 AutoLISP 코드나 노트북 셀을 단순화하십시오:

1. AGENTS.md를 읽고 프로젝트 규칙을 파악합니다.
2. 단순화 대상 AutoLISP 코드(최근에 수정된 셀이나 검토 중인 함수)를 식별합니다.
3. 수정하기 전에 코드의 용도, 도면 데이터베이스와의 상호작용, 검증용 셀을 완전히 이해합니다.
4. 다음과 같은 단순화 요소를 탐색하십시오:
   - LISP 코드 내 깊은 중첩 → `cond` 블록으로 평탄화하거나 별도의 헬퍼 함수로 추출.
   - 전역 네임스페이스 오염 → `(defun name (args / local1 local2 ...))` 선언부에 모든 임시 변수를 등록하여 지역화.
   - 명령 파이프라인의 불필요한 호출 제거 → 느린 `(command ...)` 시퀀스를 직관적인 데이터베이스 직접 접근이나 ActiveX API (`vla-Add...`)로 대체.
   - 중복된 좌표/기하학 연산 → 공통 벡터 연산이나 기하학적 유틸리티 함수로 추출.
   - 복잡한 시스템 변수 변경 → 시스템 변수를 캐싱하고 복원하는 로직을 일관된 표준 래퍼 블록으로 통합.
5. 단순화를 점진적으로 적용하고, 변경할 때마다 CAD 데이터베이스 어설션을 다시 실행합니다.
6. 모든 검증 확인이 성공적으로 완료되고 노트북이 정상적으로 로드되는지 확인합니다.

단순화 후 CAD 어설션 검증에 실패하면, 변경 사항을 즉시 되돌리고 대안을 검토하십시오. 결과 리뷰에는 `code-review-and-quality` 스킬을 사용합니다.
"""