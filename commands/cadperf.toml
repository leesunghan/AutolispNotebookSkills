description = "cad-performance-auditor 페르소나를 통한 CAD 성능 감사 실행"

prompt = """
/cadperf는 AutoLISP 및 DCL 애플리케이션을 대상으로 작동합니다. 일반적인 웹 애플리케이션 용도로 사용하지 마십시오.

## 감사 실행

`cad-performance-auditor` 하위 에이전트를 생성합니다 (CLI는 `agents/` 내의 사용자 지정 하위 에이전트를 동일한 이름의 도구로 노출합니다). 다음 정보들을 전달하십시오:

- 검토 중인 AutoLISP 함수, 선택 세트 또는 노트북 셀.
- 실행 시간 성능 지표 (예: MILLISECS 델타값) 또는 도면 객체 규모.

하위 에이전트는 점수표, 등급별 권장 사항(High, Medium, Low), 긍정적 관찰 항목 및 선제적 개선 제안 목록을 반환합니다.

## 출력

최종 감사 보고서를 사용자에게 그대로 출력합니다. 이 명령은 단일 페르소나 명령이므로 별도의 병합 및 합성 단계가 필요하지 않습니다.
"""
